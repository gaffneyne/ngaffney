---
title: Nicholas Gaffney
---

{% extends "layout.njk" %}

{% block content %}

<ul id="gallery">
  {% set counter = 0 %}
  {% for post in collections.flatPosts %}
    {% if counter < 50 %}
      <li>
        {{ post.content | safe }}
        <a href="{{ post.url }}">→</a>
      </li>
      {% set counter = counter + 1 %}
    {% endif %}
  {% endfor %}
</ul>

{% endblock %}