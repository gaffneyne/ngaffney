{% extends "layout.njk" %}
{% block content %}
<h1>🗂 Archive for {{ archive.label }} {{ archive.year }}</h1>
<ul>
  {% for post in archive.posts %}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
  {% endfor %}
</ul>
{% endblock %}
